"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
require("dotenv/config");
const http_1 = require("http");
const app_1 = require("./app");
const PORT = process.env.APP_PORT || "8080";
const server = (0, http_1.createServer)(app_1.app);
server.listen(PORT);
server.on("listening", () => console.log(`server is listening on port http://localhost:${PORT}`));
server.on("error", (error) => console.log(`server is not listening on port${PORT}: [Error]: ${error}`));
